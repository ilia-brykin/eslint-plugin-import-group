export const sortAscValid = `
import {
  computed,
  h,
  ref,
} from "vue";
`;

export const sortDescValid = `
import {
  ref,
  h,
  computed,
} from "vue";
`;

export const sortNoneValid = `
import {
  h,
  ref,
  computed,
} from "vue";
`;
